# Shared Powerlevel10k configuration.
#
# Run `p10k configure` inside the Dolphin shell, then run
# `bash sync_p10k_config.sh export` on the host to replace this file with the
# generated configuration. Commit that result to use the same prompt on every
# server.
