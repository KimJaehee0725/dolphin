.DS_Store

# Local runtime secrets. Commit runtime.env.example instead.
/runtime*.env
!/runtime.env.example

# Ignore only the old runtime location long enough for automatic migration.
/config/runtime*.env
